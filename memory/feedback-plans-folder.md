---
name: Plans folder preference
description: Daily plan files go in plans/ not days/ — user changed the folder name
type: feedback
---

Daily plan/log files are stored in `plans/YYYY-MM-DD.md`, not `days/YYYY-MM-DD.md`.

**Why:** User didn't like the `days/` folder name.

**How to apply:** All `/plan` generation and file references should use the `plans/` directory.
